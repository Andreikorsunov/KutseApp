@model IEnumerable<KutseApp.Models.Pidu>

@{
    ViewBag.Title = "Pidu";
}

<h2>Pidu</h2>

<p>
    @Html.ActionLink("Loo uus", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Uuenda", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Üksikasjad", "Details", new { id=item.Id }) |
            @Html.ActionLink("Kustutada", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>